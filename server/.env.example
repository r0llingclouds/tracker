# Anthropic API key for AI food parsing
# Get your key at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Perplexity API key for meal lookup (optional)
# Get your key at https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# Base directory for all tracker data (optional - defaults to server/data/ if not set)
# Use this to store data in iCloud or another sync location
# Each tracker stores its data in a subfolder:
#   - food/     (foods.json, food-logs.json, daily.json)
#   - habits/   (habits.json)
#   - tasks/    (tasks.json)
#   - workout/  (kettlebell.json, pushups.json, workout-daily.json)
TRACKER_DATA_DIR=/path/to/your/tracker-data/

# Backup directory for automatic daily backups (optional)
# Backups are created daily and stored as timestamped folders
# Maximum 5 backups are retained; oldest are automatically deleted
# If not set, automatic backups are disabled
BACKUP_DIR=/path/to/your/backup-directory/

# Hour to run automatic backup (0-23, optional - defaults to 2 for 2:00 AM)
BACKUP_HOUR=2
